<table>
	<% @recipes.each do |recipe| %>
	<tr>
		<td><%= link_to(image_tag(recipe["image_url"], size: "100x100")) %></td>
		<td><%= link_to(recipe["title"].html_safe) %></td>
		<td><%= recipe["social_rank"] %></td>
	</tr>
	<% end %>
</table>